apiVersion: v1
kind: Service
metadata:
  name: webapp-svc
  namespace: webapp
spec:
  selector:
    app.kubernetes.io/name: django-text-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080